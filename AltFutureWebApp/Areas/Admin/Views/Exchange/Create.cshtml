@model ExchangeViewModel

@{
    ViewData["Title"] = "Create an Exchange or Wallet";
}

<h1>Create</h1>

<h4>Exchange or Wallet</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ExchangeName" class="control-label"></label>
                <input asp-for="ExchangeName" class="form-control" />
                <span asp-validation-for="ExchangeName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
